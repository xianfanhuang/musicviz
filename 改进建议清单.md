# Sonoria AI - 改进建议清单（优先级排序）

## 🔴 P0 - 关键优化（立即实施）

### 1. Canvas离屏渲染优化
- **问题**：每帧全屏fillRect，GPU开销大
- **方案**：创建离屏Canvas，分层渲染
- **收益**：提升15-20fps

### 2. BPM实际检测算法
- **问题**：当前仅硬编码120/128
- **方案**：自相关/峰值检测计算真实BPM
- **收益**：情绪识别准确率提升40%

### 3. 非活跃标签页暂停
- **问题**：后台持续渲染耗电
- **方案**：visibilitychange监听暂停

---

## 🟠 P1 - 重要功能（本周完成）

### 4. 播放列表持久化（IndexedDB）
- 本地存储播放历史
- 支持离线歌单

### 5. 多种可视化模式切换
- 粒子/频谱/波形三模式
- 用户参数自定义

### 6. 错误边界处理
- 全局error监听
- 自动恢复机制

---

## 🟡 P2 - 体验增强（下周完成）

### 7. Web Worker音频分析
- FFT计算移至Worker线程
- 主线程更流畅

### 8. 歌词同步显示
- LRC格式解析
- KRC格式支持（酷狗）

### 9. 截图/可视化导出
- 分享到社交平台
- 生成动态GIF

### 10. 国际化支持
- 中英文切换
- i18n框架接入

---

## 代码示例

### 离屏渲染
```javascript
class GenerativeVisualizer {
    constructor() {
        this.canvas = document.getElementById('visualizer');
        this.ctx = this.canvas.getContext('2d');
        
        // 创建离屏Canvas
        this.offscreen = document.createElement('canvas');
        this.offCtx = this.offscreen.getContext('2d');
        this.resize();
    }
    
    render() {
        // 先绘制到离屏Canvas
        this.offCtx.fillStyle = 'rgba(0,0,0,0.1)';
        this.offCtx.fillRect(0, 0, this.offscreen.width, this.offscreen.height);
        // ... 绘制粒子
        
        // 一次性拷贝到主Canvas
        this.ctx.drawImage(this.offscreen, 0, 0);
    }
}
```

### BPM检测
```javascript
class AudioAnalyzer {
    detectBPM(onsets) {
        const intervals = [];
        for (let i = 1; i < onsets.length; i++) {
            if (onsets[i]) intervals.push(onsets[i] - onsets[i-1]);
        }
        if (!intervals.length) return 120;
        const avgInterval = intervals.reduce((a,b)=>a+b) / intervals.length;
        return Math.round(60000 / avgInterval);
    }
}
```

---

**生成时间**：2024年
**分析范围**：optimized_sonoria.html (1262行)
