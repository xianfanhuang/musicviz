# Sonoria AI 修复说明

## 核心问题修复

### 1. 按钮点击无响应 ✅
**原因**：原代码使用 `onclick="functionName()"` 内联绑定 + Puter SDK 同步阻塞循环

**修复**：
- 改用 `addEventListener` 替代所有内联 onclick
- Puter SDK 改为非阻塞异步初始化（移除 while 循环）
- 添加 `-webkit-user-select: none` 防止按钮文字选中干扰
- 添加 `touch-action: manipulation` 优化移动端触摸响应
- 按钮添加 `:active` 状态反馈

### 2. Canvas 离屏渲染优化 ✅
**原问题**：每帧直接绘制到主 canvas + 全屏 `fillRect`

**修复**：
- 创建离屏 canvas（`document.createElement('canvas')`）
- 所有渲染先在 offscreen 完成
- 最后一次性 `drawImage` 到主 canvas
- **预期提升**：15-20 FPS

### 3. BPM 实际检测算法 ✅
**原问题**：BPM 固定为 120/128

**修复**：
- 新增 `BPMDetector` 类实现 onset-based 检测
- 基于能量差分检测节拍
- 使用中位数间隔计算 BPM（抗噪声）
- 检测范围：40-200 BPM

### 4. IndexedDB 播放列表持久化 ✅
**原问题**：无持久化

**修复**：
- 新增 `PlaylistDB` 类封装 IndexedDB 操作
- `addTrack()` / `getAllTracks()` / `removeTrack()`
- 播放列表支持添加、切换、顺序播放

## 附加优化

- **后台暂停**：visibilitychange 监听，后台自动暂停
- **播放列表 UI**：侧边滑出面板
- **IIFE 封装**：所有代码包裹在匿名函数中，避免全局污染
- **DOMContentLoaded**：确保 DOM 加载完成后再绑定事件
